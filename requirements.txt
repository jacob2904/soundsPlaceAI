# Core pipeline dependencies for soundsPlaceAI / CineSFX.
# ffmpeg must be installed as a system binary and available on PATH.

# Scene / shot detection and frame handling
scenedetect>=0.6.4
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# Configuration and HTTP
PyYAML>=6.0
python-dotenv>=1.0.0
httpx>=0.27.0
requests>=2.31.0

# Offline license verification (Ed25519 signatures)
cryptography>=41.0.0

# Optional "brain" SDKs. These are imported lazily; install only the one you use.
# google-generativeai>=0.7.0
# openai>=1.30.0
# anthropic>=0.30.0
